django_settings_module: "regluit.settings.me"
project_path: "/opt/regluit"
virtualenv_name: "venv"
django_server_ip: "0.0.0.0"
django_server_port: 8000

# Untested: the hosts this role served are dead (2026-08-30). Kept at the
# value these tasks were written for; see roles/regluit_prod for the live role.
python_version: "3.8"
