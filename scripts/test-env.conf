# Shared configuration for test.unglue.it scripts
# Source this from other scripts: source "$(dirname "$0")/test-env.conf"

INSTANCE_ID="i-0bf3dbf96d0f25b2d"
RDS_INSTANCE="test-2024"
SNAPSHOT_PREFIX="test-2024-teardown"
RDS_SUBNET_GROUP="default-vpc-bb9a3cc0"
RDS_SECURITY_GROUP="sg-ff9a4181"
REGION="us-east-1"
PROVISIONING_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
SSH_KEY="/Volumes/ryvault1/gluejar/EC2_Keys/production.pem"
VAULT_PASS="/Volumes/ryvault1/ebookfoundation/vault_pass.txt"

# AWS credentials via 1Password
# rdhyee IAM user in EbookFoundation account (has EC2/RDS/Route53 access)
export AWS_ACCESS_KEY_ID="op://EbookFoundation/m6yyewunl4koaf7mvpta4rklzy/username"
export AWS_SECRET_ACCESS_KEY="op://EbookFoundation/m6yyewunl4koaf7mvpta4rklzy/credential"
export AWS_DEFAULT_REGION="$REGION"

# Ensure we use op run for AWS calls
_aws() {
    op run -- aws "$@"
}
